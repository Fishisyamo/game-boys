<% provide(:title, "Top") %>
<% content_for :cover do %>
  <div class="cover">
    <div class="cover-inner">
      <div class="cover-contents">
        <img class="cover-logo" src="/images/cover_logo.png" alt="Gameboys">
        <h1>ゲームの興奮を仲間と共に</h1>
        <% unless logged_in? %>
          <%= link_to 'Press Start', signup_path, class: 'btn btn-danger btn-lg'%>
        <% end %>
      </div>
    </div>
  </div>
<% end %>

<p>テスト</p>